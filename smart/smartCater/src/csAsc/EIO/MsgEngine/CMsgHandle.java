package csAsc.EIO.MsgEngine;

public class CMsgHandle
{//消息处理的接口的实现类的父类，所有的消息处理类都要从该类派生
 
 /*
 public String genRetMsgHead(String veName,String msgType,String msgName)
 {//生成响应消息的头，用户可以重载其以发挥不同格式的消息头	 
  return  reqVeNameId+":\""+ reqVeName +"\"," + reqMsgType+":\""+ reqMsgName + "\"";
 }
 */
}
